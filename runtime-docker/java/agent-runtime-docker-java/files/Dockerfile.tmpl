FROM eclipse-temurin:21-jdk
WORKDIR /workspace
COPY . .
EXPOSE 8080
CMD ["sh", "-c", "echo 'Build the Java agent jar and replace this command with the packaged runtime entrypoint.' && sleep infinity"]

services:
  {{ kebab .AgentName }}:
    build:
      context: ../..
      dockerfile: agents/{{ .AgentName }}/Dockerfile
    environment:
      PORT: "8080"
      LOG_LEVEL: info
    ports:
      - "18080:8080"
